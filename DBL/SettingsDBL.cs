using DAL;
using POCO;
using System;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace DBL
{
    public class SettingsDBL
    {
        //public Settings M_Settings_GetByBranchId(int BranchId)
        //{
        //    DataSet ds = new DataSet();
        //    Settings oSettings = new Settings();
        //    SettingsDAL oSettingsDAL = new SettingsDAL();
            
        //    ds = oSettingsDAL.M_Settings_GetByBranchId(BranchId);

        //    if (ds.Tables.Count > 0)
        //    {
        //        if (ds.Tables[0].Rows.Count > 0)
        //        {
        //            oSettings. = int.Parse(ds.Tables[0].Rows[0][""].ToString());
        //            oSettings. = ds.Tables[0].Rows[0][""].ToString();
                    

        //        }

        //    }
        //    return oSettings;
        //}
    }
}
